(function(){
	
  var $loginForm = $('#work');
  var $form = $('<%= j render 'info' %>');

  if ($loginForm.size() > 0) {
    $loginForm.modal();
  } else {
    $('body').append($form);
    $form.modal();
  }
})();